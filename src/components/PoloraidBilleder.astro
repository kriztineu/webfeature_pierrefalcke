<div class="wrapper">

    <div class="container">

        <section class="billeder">
            <img src="src/assets/images/stock1.webp" alt="">  
        </section>

        <section class="billeder">
            <img src="src/assets/images/stock2.webp" alt="">
        </section>

        <section class="billeder">
            <img src="src/assets/images/stock3.webp" alt="">
        </section>

        <section class="billeder">
            <img src="src/assets/images/stock4.webp" alt="">
        </section>

        <section class="billeder">
            <img src="src/assets/images/stock5.webp" alt="">
        </section>
    </div>
</div>

    


<style>
    .wrapper {
        overflow-x: hidden;
        position: relative;
    }
    .container {
        display: flex;
        gap: var(--size-2);
        width: 250vw;
    }
    
    .container .billeder img {
        aspect-ratio: var(--ratio-box);
    }
</style>

<script>
import { gsap } from "gsap/dist/gsap";
import { ScrollTrigger } from "gsap/dist/ScrollTrigger";
        
gsap.registerPlugin(ScrollTrigger);

const sections = gsap.utils.toArray('.container section')

gsap.to(sections, {
      xPercent: -100 * (sections.length - 1),
      ease: "none",
      scrollTrigger: {
        trigger: ".container",
        pin: true,
        scrub: 1, 
        end: "+=3000",
      }
})
</script>
