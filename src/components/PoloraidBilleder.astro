<div class="wrapper">

    <div class="container-h">

        <section class="panel">
            <figure>
            <img src="src/assets/images/nystock1.png" alt="">
            </figure>
        </section>
        <section class="panel">
            <figure>
            <img src="src/assets/images/polo.png" alt="">
            </figure>
        </section>
        <section class="panel">
            <figure>
            <img src="src/assets/images/polo.png" alt="">
            </figure>
        </section>
        <section class="panel">
            <figure>
            <img src="src/assets/images/polo.png" alt="">
            </figure>
        </section>
        <section class="panel">
            <figure>
            <img src="src/assets/images/polo.png" alt="">
            </figure>
        </section>

        <!-- <section class="billeder">
            <img src="src/assets/images/polaroid.png" alt="">  
        </section> -->
        <!-- <section>
            <img src="src/assets/images/polaroid2.png" alt="">  
        </section> -->
    </div>
</div>

    


<style>
    .wrapper {
        overflow-x: hidden;
        position: relative;
    }
    .container-h {
        display: flex;
        gap: var(--size-2);
    }

    .panel {
        width: 100vw;
        flex-shrink: 0;
    }
    :global(body) {
        overflow-x: hidden;
    }
</style>

<script>
import { gsap } from "gsap";
import { ScrollTrigger } from "gsap/ScrollTrigger";

gsap.registerPlugin(ScrollTrigger);

const sections = gsap.utils.toArray('.panel')

gsap.to(sections, {
      xPercent: -100 * (sections.length - 1),
      ease: "none",
      scrollTrigger: {
        trigger: ".container-h",
        pin: true,
        scrub: true, 
        end: "+=3000",
      }
})
</script>
